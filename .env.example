# Copy to `.env` and fill in values. See README.md for details.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# MEDICAL_DATA_DIR=

# --- Docker Compose (optional) ---
# API_PORT=8000
# CHAT_PORT=8501
# When running the chat container, default API URL inside the stack:
# ORCHESTRATOR_BASE_URL=http://api:8000
